services:
  app:
    build: .
    image: eng-vocab
    container_name: eng-vocab
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
